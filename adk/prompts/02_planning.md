# Prompt: planning

Purpose: prepare the work order and split it into stages.

To refine later:
- short-term plan,
- iterative plan,
- dependencies between stages,
- validation checkpoints,
- stage completion criteria.

Note:
- this stage should build a task list for a chain or graph of tasks,
- it should allow returning to earlier steps when needed.