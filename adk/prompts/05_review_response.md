# Prompt: response to feedback

Purpose: react to feedback from a supervisor, reviewer, or user.

To refine later:
- detect the fragment that needs work,
- classify the feedback,
- propose a fix,
- estimate the impact on other files,
- describe what was done.

Note:
- the system should distinguish content fixes, code fixes, formatting fixes, and diagram fixes.